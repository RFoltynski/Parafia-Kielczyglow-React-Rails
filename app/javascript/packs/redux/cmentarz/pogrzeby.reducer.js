const INITIAL_STATE = {
  pogrzeby: "w budowie",
};

const pogrzebyReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default pogrzebyReducer;
