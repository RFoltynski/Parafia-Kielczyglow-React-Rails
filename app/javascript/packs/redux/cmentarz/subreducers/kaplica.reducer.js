const INITIAL_STATE = {
  mainHeader: "Kaplica",
  kaplica: "w budowie",
};

const kaplicaReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default kaplicaReducer;
