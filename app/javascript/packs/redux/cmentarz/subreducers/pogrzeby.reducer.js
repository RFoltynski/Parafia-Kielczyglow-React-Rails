const INITIAL_STATE = {
  mainHeader: "Pogrzeby",
  pogrzeby: "w budowie",
};

const pogrzebyReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default pogrzebyReducer;
