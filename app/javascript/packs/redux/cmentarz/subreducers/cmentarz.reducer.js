const INITIAL_STATE = {
  mainHeader: "Cmentarz",
  cmentarz: "W budowie",
};

const cmentarzReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cmentarzReducer;
